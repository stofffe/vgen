{{ define "req" }}
{{ end }}

{{ define "not_empty" }}
if !(len({{ .LowerName }}) > 0) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("can not be empty"))
}
{{ end }}

{{ define "custom" }}
{{ end }}

{{ define "gt" }}
if !({{ .LowerName }} > {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("must be > {{ .Param }}"))
}
{{ end }}

{{ define "lt" }}
if !({{ .LowerName }} < {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("must be < {{ .Param }}"))
}
{{ end }}

{{ define "gte" }}
if !({{ .LowerName }} >= {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("must be >= {{ .Param }}"))
}
{{ end }}

{{ define "lte" }}
if !({{ .LowerName }} <= {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("must be <= {{ .Param }}"))
}
{{ end }}

{{ define "len_gt" }}
if !(len({{ .LowerName }}) > {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("len must be > {{ .Param }}"))
}
{{ end }}

{{ define "len_lt" }}
if !(len({{ .LowerName }}) < {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("len must be < {{ .Param }}"))
}
{{ end }}

{{ define "len_gte" }}
if !(len({{ .LowerName }}) >= {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("len must be >= {{ .Param }}"))
}
{{ end }}

{{ define "len_lte" }}
if !(len({{ .LowerName }}) >= {{ .Param }}) {
    errs[{{ .Path }}] = append(errs[{{ .Path }}], fmt.Sprintf("len must be >= {{ .Param }}"))
}
{{ end }}
